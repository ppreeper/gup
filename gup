#!/usr/bin/env bash
# This script was generated by bashly 1.3.8 (https://bashly.dev)
# Modifying it manually is not recommended

# :wrapper.bash3_bouncer
if ((BASH_VERSINFO[0] < 4 || (BASH_VERSINFO[0] == 4 && BASH_VERSINFO[1] < 2))); then
	printf "bash version 4.2 or higher is required\n" >&2
	exit 1
fi

# :command.master_script

# :command.version_command
version_command() {
	echo "$version"
}

# :command.usage
gup_usage() {
	printf "gup - github app installer\n\n"

	printf "%s\n" "Usage:"
	printf "  gup COMMAND\n"
	printf "  gup [COMMAND] --help | -h\n"
	printf "  gup --version | -v\n"
	echo
	# :command.usage_commands
	printf "%s\n" "Commands:"
	printf "  %s   install an application\n" "install"
	printf "  %s   remove an application and settings\n" "remove "
	printf "  %s   install systemd service\n" "systemd"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo
		printf "  %s\n" "--version, -v"
		printf "    Show version number\n"
		echo

	fi
}

# :command.usage
gup_install_usage() {
	printf "gup install - install an application\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install COMMAND\n"
	printf "  gup install [COMMAND] --help | -h\n"
	echo
	# :command.usage_commands
	printf "%s\n" "Commands:"
	printf "  %s   install gup command\n" "gup              "
	printf "  %s   update script for apt update\n" "update           "
	echo
	printf "%s\n" "Language Development Commands:"
	printf "  %s   go programming language\n" "go               "
	printf "  %s   tinygo is a Go compiler for small places\n" "tinygo           "
	printf "  %s   nodejs js engine\n" "nodejs           "
	printf "  %s   Node Version Manager\n" "nvm              "
	printf "  %s   deno js engine\n" "deno             "
	printf "  %s   bun js engine\n" "bun              "
	printf "  %s   extremely fast Python package installer and resolver\n" "uv               "
	printf "  %s   rust programming language\n" "rust             "
	printf "  %s   zig programming language\n" "zig              "
	echo
	printf "%s\n" "Go Development Tools Commands:"
	printf "  %s   Go-blueprint allows users to spin up a quick Go project using a popular framework\n" "go-blueprint     "
	printf "  %s   CLI tool to create amazing web applications with Go\n" "gowebly          "
	printf "  %s   The Go Playground client\n" "goplay           "
	printf "  %s   Generate Go tests from your source code\n" "gotests          "
	printf "  %s   impl generates method stubs for implementing an interface\n" "impl             "
	printf "  %s   Go tool to modify/update field tags in structs\n" "gomodifytags     "
	printf "  %s   Live reload for Go apps\n" "air              "
	printf "  %s   gofumpt is a stricter gofmt\n" "gofumpt          "
	printf "  %s   Go tool to format Go source code and manage imports\n" "goimports        "
	printf "  %s   golangci-lint is a linters aggregator for Go\n" "golangci-lint    "
	printf "  %s   The advanced Go linter\n" "staticcheck      "
	printf "  %s   nilaway is a tool to detect nil pointer dereferences in Go code\n" "nilaway          "
	printf "  %s   Automatically generate RESTful API documentation with Swagger 2.0 for Go\n" "swag             "
	printf "  %s   An HTML templating language for Go that has great developer tooling\n" "templ            "
	echo
	printf "%s\n" "Linter / Formatter / Code Quality Commands:"
	printf "  %s   an extremely fast Python linter and code formatter\n" "ruff             "
	printf "  %s   Sloc Cloc and Code (scc)\n" "scc              "
	printf "  %s   an extensible command line tool or library to format yaml files.\n" "yamlfmt          "
	echo
	printf "%s\n" "Language Servers & Debuggers Commands:"
	printf "  %s   Go language server\n" "gopls            "
	printf "  %s   Delve is a source level debugger for Go programs\n" "dlv              "
	printf "  %s   zig language server\n" "zls              "
	echo
	printf "%s\n" "Editors & IDEs Commands:"
	printf "  %s   NeoVIM\n" "neovim           "
	printf "  %s   helix text editor\n" "helix            "
	echo
	printf "%s\n" "Observability & Monitoring Commands:"
	printf "  %s   prometheus\n" "prometheus       "
	printf "  %s   grafana\n" "grafana          "
	printf "  %s   loki\n" "loki             "
	printf "  %s   node_exporter\n" "node_exporter    "
	printf "  %s   postgres_exporter\n" "postgres_exporter"
	printf "  %s   blackbox_exporter\n" "blackbox_exporter"
	printf "  %s   snmp_exporter\n" "snmp_exporter    "
	printf "  %s   btop\n" "btop             "
	printf "  %s   gotop terminal based graphical activity monitor\n" "gotop            "
	echo
	printf "%s\n" "Containers & Orchestration Commands:"
	printf "  %s   A simple terminal UI for docker and docker-compose\n" "lazydocker       "
	printf "  %s   minikube\n" "minikube         "
	printf "  %s   kubectl\n" "kubectl          "
	printf "  %s   Kubernetes CLI\n" "k9s              "
	printf "  %s   etcd\n" "etcd             "
	printf "  %s   HashiCorp Vagrant\n" "vagrant          "
	echo
	printf "%s\n" "Cloud / Hosting CLI Tools Commands:"
	printf "  %s   DigitalOcean cli tool\n" "doctl            "
	printf "  %s   Backblaze B2 cli tool\n" "b2               "
	printf "  %s   github cli\n" "githubcli        "
	printf "  %s   forgejo cli\n" "forgejocli       "
	echo
	printf "%s\n" "Web / Frontend / Site / Proxies Commands:"
	printf "  %s   caddy2 web server\n" "caddy2           "
	printf "  %s   traefik\n" "traefik          "
	printf "  %s   hugo website generator\n" "hugo             "
	printf "  %s   tailwindcss cli\n" "tailwindcss      "
	printf "  %s   Marp CLI is a Markdown presentation tool\n" "marp             "
	echo
	printf "%s\n" "Utilities & CLI Helpers Commands:"
	printf "  %s   Disk Usage/Free Utility\n" "duf              "
	printf "  %s   Fast disk usage analyzer with console interface written in Go\n" "gdu              "
	printf "  %s   A tool for glamorous shell scripts\n" "gum              "
	printf "  %s   A simple terminal UI for git commands\n" "lazygit          "
	printf "  %s   A fast, concurrent web link checker\n" "muffet           "
	printf "  %s   s5cmd is a super fast and highly parallel S3 and local filesystem execution\n" "s5cmd            "
	printf "  %s   A tool to manage SSH configuration\n" "sshconfig        "
	printf "  %s   A task runner / simpler Make alternative written in Go\n" "task             "
	printf "  %s   A sleep command with a progress bar\n" "timer            "
	printf "  %s   A command-line program to download videos from YouTube and other sites\n" "ytdlp            "
	printf "  %s   A portable, performant script to make rolling ZFS snapshots easy\n" "zfsnap           "
	echo
	printf "%s\n" "Infrastructure & DevOps Commands:"
	printf "  %s   Terraform is an open-source infrastructure as code software tool\n" "terraform        "
	printf "  %s   Distrobuilder is a tool to build Linux distributions and images\n" "distrobuilder    "
	echo
	printf "%s\n" "Packaging / Binary Tools Commands:"
	printf "  %s   Ultimate Packer for eXecutables\n" "upx              "
	echo
	printf "%s\n" "Database & Code Generation Commands:"
	printf "  %s   Generate type-safe code from SQL\n" "sqlc             "
	echo
	printf "%s\n" "Security & Vulnerability Scanning Commands:"
	printf "  %s   mkcert is a simple tool for making locally-trusted development certificates\n" "mkcert           "
	printf "  %s   Go vulnerability checker\n" "govulncheck      "
	echo
	printf "%s\n" "Fonts & Assets Commands:"
	printf "  %s   nerdfonts installer\n" "nerdfonts        "
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

		# :command.usage_examples
		printf "%s\n" "Examples:"
		printf "  gup install neovim\n"
		echo

	fi
}

# :command.usage
gup_install_gup_usage() {
	printf "gup install gup - install gup command\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gup\n"
	printf "  gup install gup --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_update_usage() {
	printf "gup install update - update script for apt update\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install update\n"
	printf "  gup install update --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_go_usage() {
	printf "gup install go - go programming language\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install go\n"
	printf "  gup install go --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_tinygo_usage() {
	printf "gup install tinygo - tinygo is a Go compiler for small places\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install tinygo\n"
	printf "  gup install tinygo --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_nodejs_usage() {
	printf "gup install nodejs - nodejs js engine\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install nodejs\n"
	printf "  gup install nodejs --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_nvm_usage() {
	printf "gup install nvm - Node Version Manager\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install nvm\n"
	printf "  gup install nvm --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_deno_usage() {
	printf "gup install deno - deno js engine\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install deno\n"
	printf "  gup install deno --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_bun_usage() {
	printf "gup install bun - bun js engine\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install bun\n"
	printf "  gup install bun --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_uv_usage() {
	printf "gup install uv - extremely fast Python package installer and resolver\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install uv\n"
	printf "  gup install uv --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_rust_usage() {
	printf "gup install rust - rust programming language\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install rust\n"
	printf "  gup install rust --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_zig_usage() {
	printf "gup install zig - zig programming language\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install zig\n"
	printf "  gup install zig --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_go_blueprint_usage() {
	printf "gup install go-blueprint - Go-blueprint allows users to spin up a quick Go project using a popular framework\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install go-blueprint\n"
	printf "  gup install go-blueprint --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_gowebly_usage() {
	printf "gup install gowebly - CLI tool to create amazing web applications with Go\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gowebly\n"
	printf "  gup install gowebly --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_goplay_usage() {
	printf "gup install goplay - The Go Playground client\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install goplay\n"
	printf "  gup install goplay --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_gotests_usage() {
	printf "gup install gotests - Generate Go tests from your source code\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gotests\n"
	printf "  gup install gotests --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_impl_usage() {
	printf "gup install impl - impl generates method stubs for implementing an interface\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install impl\n"
	printf "  gup install impl --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_gomodifytags_usage() {
	printf "gup install gomodifytags - Go tool to modify/update field tags in structs\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gomodifytags\n"
	printf "  gup install gomodifytags --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_air_usage() {
	printf "gup install air - Live reload for Go apps\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install air\n"
	printf "  gup install air --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_gofumpt_usage() {
	printf "gup install gofumpt - gofumpt is a stricter gofmt\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gofumpt\n"
	printf "  gup install gofumpt --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_goimports_usage() {
	printf "gup install goimports - Go tool to format Go source code and manage imports\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install goimports\n"
	printf "  gup install goimports --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_golangci_lint_usage() {
	printf "gup install golangci-lint - golangci-lint is a linters aggregator for Go\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install golangci-lint\n"
	printf "  gup install golangci-lint --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_staticcheck_usage() {
	printf "gup install staticcheck - The advanced Go linter\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install staticcheck\n"
	printf "  gup install staticcheck --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_nilaway_usage() {
	printf "gup install nilaway - nilaway is a tool to detect nil pointer dereferences in Go code\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install nilaway\n"
	printf "  gup install nilaway --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_swag_usage() {
	printf "gup install swag - Automatically generate RESTful API documentation with Swagger 2.0 for Go\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install swag\n"
	printf "  gup install swag --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_templ_usage() {
	printf "gup install templ - An HTML templating language for Go that has great developer tooling\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install templ\n"
	printf "  gup install templ --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_ruff_usage() {
	printf "gup install ruff - an extremely fast Python linter and code formatter\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install ruff\n"
	printf "  gup install ruff --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_scc_usage() {
	printf "gup install scc - Sloc Cloc and Code (scc)\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install scc\n"
	printf "  gup install scc --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_yamlfmt_usage() {
	printf "gup install yamlfmt - an extensible command line tool or library to format yaml files.\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install yamlfmt\n"
	printf "  gup install yamlfmt --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_gopls_usage() {
	printf "gup install gopls - Go language server\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gopls\n"
	printf "  gup install gopls --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_dlv_usage() {
	printf "gup install dlv - Delve is a source level debugger for Go programs\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install dlv\n"
	printf "  gup install dlv --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_zls_usage() {
	printf "gup install zls - zig language server\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install zls\n"
	printf "  gup install zls --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_neovim_usage() {
	printf "gup install neovim - NeoVIM\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install neovim\n"
	printf "  gup install neovim --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_helix_usage() {
	printf "gup install helix - helix text editor\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install helix\n"
	printf "  gup install helix --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_prometheus_usage() {
	printf "gup install prometheus - prometheus\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install prometheus\n"
	printf "  gup install prometheus --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_grafana_usage() {
	printf "gup install grafana - grafana\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install grafana\n"
	printf "  gup install grafana --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_loki_usage() {
	printf "gup install loki - loki\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install loki\n"
	printf "  gup install loki --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_node_exporter_usage() {
	printf "gup install node_exporter - node_exporter\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install node_exporter\n"
	printf "  gup install node_exporter --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_postgres_exporter_usage() {
	printf "gup install postgres_exporter - postgres_exporter\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install postgres_exporter\n"
	printf "  gup install postgres_exporter --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_blackbox_exporter_usage() {
	printf "gup install blackbox_exporter - blackbox_exporter\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install blackbox_exporter\n"
	printf "  gup install blackbox_exporter --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_snmp_exporter_usage() {
	printf "gup install snmp_exporter - snmp_exporter\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install snmp_exporter\n"
	printf "  gup install snmp_exporter --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_btop_usage() {
	printf "gup install btop - btop\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install btop\n"
	printf "  gup install btop --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_gotop_usage() {
	printf "gup install gotop - gotop terminal based graphical activity monitor\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gotop\n"
	printf "  gup install gotop --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_lazydocker_usage() {
	printf "gup install lazydocker - A simple terminal UI for docker and docker-compose\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install lazydocker\n"
	printf "  gup install lazydocker --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_minikube_usage() {
	printf "gup install minikube - minikube\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install minikube\n"
	printf "  gup install minikube --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_kubectl_usage() {
	printf "gup install kubectl - kubectl\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install kubectl\n"
	printf "  gup install kubectl --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_k9s_usage() {
	printf "gup install k9s - Kubernetes CLI\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install k9s\n"
	printf "  gup install k9s --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_etcd_usage() {
	printf "gup install etcd - etcd\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install etcd\n"
	printf "  gup install etcd --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_vagrant_usage() {
	printf "gup install vagrant - HashiCorp Vagrant\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install vagrant\n"
	printf "  gup install vagrant --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_doctl_usage() {
	printf "gup install doctl - DigitalOcean cli tool\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install doctl\n"
	printf "  gup install doctl --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_b2_usage() {
	printf "gup install b2 - Backblaze B2 cli tool\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install b2\n"
	printf "  gup install b2 --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_githubcli_usage() {
	printf "gup install githubcli - github cli\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install githubcli\n"
	printf "  gup install githubcli --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_forgejocli_usage() {
	printf "gup install forgejocli - forgejo cli\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install forgejocli\n"
	printf "  gup install forgejocli --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_caddy2_usage() {
	printf "gup install caddy2 - caddy2 web server\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install caddy2\n"
	printf "  gup install caddy2 --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_traefik_usage() {
	printf "gup install traefik - traefik\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install traefik\n"
	printf "  gup install traefik --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_hugo_usage() {
	printf "gup install hugo - hugo website generator\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install hugo\n"
	printf "  gup install hugo --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_tailwindcss_usage() {
	printf "gup install tailwindcss - tailwindcss cli\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install tailwindcss\n"
	printf "  gup install tailwindcss --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_marp_usage() {
	printf "gup install marp - Marp CLI is a Markdown presentation tool\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install marp\n"
	printf "  gup install marp --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_duf_usage() {
	printf "gup install duf - Disk Usage/Free Utility\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install duf\n"
	printf "  gup install duf --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_gdu_usage() {
	printf "gup install gdu - Fast disk usage analyzer with console interface written in Go\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gdu\n"
	printf "  gup install gdu --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_gum_usage() {
	printf "gup install gum - A tool for glamorous shell scripts\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install gum\n"
	printf "  gup install gum --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_lazygit_usage() {
	printf "gup install lazygit - A simple terminal UI for git commands\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install lazygit\n"
	printf "  gup install lazygit --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_muffet_usage() {
	printf "gup install muffet - A fast, concurrent web link checker\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install muffet\n"
	printf "  gup install muffet --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_s5cmd_usage() {
	printf "gup install s5cmd - s5cmd is a super fast and highly parallel S3 and local filesystem execution\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install s5cmd\n"
	printf "  gup install s5cmd --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_sshconfig_usage() {
	printf "gup install sshconfig - A tool to manage SSH configuration\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install sshconfig\n"
	printf "  gup install sshconfig --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_task_usage() {
	printf "gup install task - A task runner / simpler Make alternative written in Go\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install task\n"
	printf "  gup install task --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_timer_usage() {
	printf "gup install timer - A sleep command with a progress bar\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install timer\n"
	printf "  gup install timer --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_ytdlp_usage() {
	printf "gup install ytdlp - A command-line program to download videos from YouTube and other sites\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install ytdlp\n"
	printf "  gup install ytdlp --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_zfsnap_usage() {
	printf "gup install zfsnap - A portable, performant script to make rolling ZFS snapshots easy\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install zfsnap\n"
	printf "  gup install zfsnap --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_terraform_usage() {
	printf "gup install terraform - Terraform is an open-source infrastructure as code software tool\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install terraform\n"
	printf "  gup install terraform --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_distrobuilder_usage() {
	printf "gup install distrobuilder - Distrobuilder is a tool to build Linux distributions and images\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install distrobuilder\n"
	printf "  gup install distrobuilder --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_upx_usage() {
	printf "gup install upx - Ultimate Packer for eXecutables\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install upx\n"
	printf "  gup install upx --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_sqlc_usage() {
	printf "gup install sqlc - Generate type-safe code from SQL\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install sqlc\n"
	printf "  gup install sqlc --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_mkcert_usage() {
	printf "gup install mkcert - mkcert is a simple tool for making locally-trusted development certificates\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install mkcert\n"
	printf "  gup install mkcert --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_govulncheck_usage() {
	printf "gup install govulncheck - Go vulnerability checker\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install govulncheck\n"
	printf "  gup install govulncheck --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_install_nerdfonts_usage() {
	printf "gup install nerdfonts - nerdfonts installer\n\n"

	printf "%s\n" "Usage:"
	printf "  gup install nerdfonts FONT...\n"
	printf "  gup install nerdfonts --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

		# :command.usage_args
		printf "%s\n" "Arguments:"

		# :argument.usage
		printf "  %s\n" "FONT..."
		printf "\n"
		printf "    %s\n" "Allowed: Ubuntu, UbuntuMono, Hack, FiraMono, Noto"
		echo

	fi
}

# :command.usage
gup_remove_usage() {
	printf "gup remove - remove an application and settings\n\n"

	printf "%s\n" "Usage:"
	printf "  gup remove COMMAND\n"
	printf "  gup remove [COMMAND] --help | -h\n"
	echo
	# :command.usage_commands
	printf "%s\n" "Commands:"
	printf "  %s   NeoVIM\n" "neovim"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

		# :command.usage_examples
		printf "%s\n" "Examples:"
		printf "  gup remove neovim\n"
		echo

	fi
}

# :command.usage
gup_remove_neovim_usage() {
	printf "gup remove neovim - NeoVIM\n\n"

	printf "%s\n" "Usage:"
	printf "  gup remove neovim\n"
	printf "  gup remove neovim --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_systemd_usage() {
	printf "gup systemd - install systemd service\n\n"

	printf "%s\n" "Usage:"
	printf "  gup systemd COMMAND\n"
	printf "  gup systemd [COMMAND] --help | -h\n"
	echo
	# :command.usage_commands
	printf "%s\n" "Commands:"
	printf "  %s   loki\n" "loki    "
	printf "  %s   promtail\n" "promtail"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

		# :command.usage_examples
		printf "%s\n" "Examples:"
		printf "  gup install neovim\n"
		echo

	fi
}

# :command.usage
gup_systemd_loki_usage() {
	printf "gup systemd loki - loki\n\n"

	printf "%s\n" "Usage:"
	printf "  gup systemd loki\n"
	printf "  gup systemd loki --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.usage
gup_systemd_promtail_usage() {
	printf "gup systemd promtail - promtail\n\n"

	printf "%s\n" "Usage:"
	printf "  gup systemd promtail\n"
	printf "  gup systemd promtail --help | -h\n"
	echo

	# :command.long_usage
	if [[ -n "$long_usage" ]]; then
		# :command.usage_options
		printf "%s\n" "Options:"

		# :command.usage_fixed_flags
		printf "  %s\n" "--help, -h"
		printf "    Show this help\n"
		echo

	fi
}

# :command.normalize_input
# :command.normalize_input_function
normalize_input() {
	local arg passthru flags
	passthru=false

	while [[ $# -gt 0 ]]; do
		arg="$1"
		if [[ $passthru == true ]]; then
			input+=("$arg")
		elif [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
			input+=("${BASH_REMATCH[1]}")
			input+=("${BASH_REMATCH[2]}")
		elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
			input+=("${BASH_REMATCH[1]}")
			input+=("${BASH_REMATCH[2]}")
		elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
			flags="${BASH_REMATCH[1]}"
			for ((i = 0; i < ${#flags}; i++)); do
				input+=("-${flags:i:1}")
			done
		elif [[ "$arg" == "--" ]]; then
			passthru=true
			input+=("$arg")
		else
			input+=("$arg")
		fi

		shift
	done
}

# :command.inspect_args
inspect_args() {
	local k

	if ((${#args[@]})); then
		readarray -t sorted_keys < <(printf '%s\n' "${!args[@]}" | sort)
		echo args:
		for k in "${sorted_keys[@]}"; do
			echo "- \${args[$k]} = ${args[$k]}"
		done
	else
		echo args: none
	fi

	if ((${#deps[@]})); then
		readarray -t sorted_keys < <(printf '%s\n' "${!deps[@]}" | sort)
		echo
		echo deps:
		for k in "${sorted_keys[@]}"; do
			echo "- \${deps[$k]} = ${deps[$k]}"
		done
	fi

	if ((${#env_var_names[@]})); then
		readarray -t sorted_names < <(printf '%s\n' "${env_var_names[@]}" | sort)
		echo
		echo "environment variables:"
		for k in "${sorted_names[@]}"; do
			echo "- \$$k = ${!k:-}"
		done
	fi
}

# :command.user_lib
# src/lib/helpers.sh
gup_ensure_go() {
		local missing_deps=()
		for dep in go; do
				if ! command -v "$dep" >/dev/null 2>&1; then
						missing_deps+=("$dep")
				fi
		done

		if [ ${#missing_deps[@]} -ne 0 ]; then
				echo "Missing go: please install Go (run 'gup install go')" >&2
				exit 1
		fi
}

# download using curl or wget
gup_download() {
		local url="$1"
		local output="$2"
		if command -v curl >/dev/null 2>&1; then
				curl -fsSL -o "$output" "$url"
		else
				wget -qO "$output" "$url"
		fi
}

# get the latest release tag from GitHub API
# uses curl if available, falls back to wget
gup_get_latest_release() {
		local repo="$1"
		local api_url="https://api.github.com/repos/${repo}/releases/latest"
		if command -v curl >/dev/null 2>&1; then
				curl -fsSL "${api_url}" | jq -r .tag_name
		else
				wget -qO- "${api_url}" | jq -r .tag_name
		fi
}

# create a secure temporary directory
gup_mktemp_dir() {
		mktemp -d "${TMPDIR:-/tmp}/gup.XXXXXXXXXX"
}

# gup_fetch_release repo jq_asset_filter
# Fetches latest GitHub release info with a single API call.
# Sets globals: GUP_REL_VERSION (no leading v), GUP_REL_DL, GUP_REL_FN, GUP_REL_JSON
gup_fetch_release() {
		local repo="$1"
		local jq_filter="$2"
		local api_url="https://api.github.com/repos/${repo}/releases/latest"
		if command -v curl >/dev/null 2>&1; then
				GUP_REL_JSON=$(curl -fsSL "${api_url}")
		else
				GUP_REL_JSON=$(wget -qO- "${api_url}")
		fi
		GUP_REL_VERSION=$(echo "${GUP_REL_JSON}" | jq -r '.tag_name' | sed 's/^v//')
		GUP_REL_DL=$(echo "${GUP_REL_JSON}" | jq -r ".assets[] | select(.name | ${jq_filter}) | .browser_download_url")
		GUP_REL_FN=$(echo "${GUP_REL_JSON}" | jq -r ".assets[] | select(.name | ${jq_filter}) | .name")
}

# _gup_install_binary src_path [dest_name]
# Installs a single binary with root/user detection.
# Sets BDIR globally.
_gup_install_binary() {
		local src="$1"
		local name="${2:-$(basename "$1")}"
		if [ "$(id -u)" = 0 ]; then
				BDIR="/usr/local/bin"
				sudo rm -f "${BDIR}/${name}"
				sudo install "${src}" "${BDIR}/${name}"
		else
				BDIR="${HOME}/.local/bin"
				rm -f "${BDIR}/${name}"
				install "${src}" "${BDIR}/${name}"
		fi
}

# _gup_install_binaries src_dir name [name ...]
# Installs multiple binaries from a directory with root/user detection.
# Sets BDIR globally.
_gup_install_binaries() {
		local src_dir="$1"
		shift
		if [ "$(id -u)" = 0 ]; then
				BDIR="/usr/local/bin"
				for name in "$@"; do
						sudo rm -f "${BDIR}/${name}"
						sudo install "${src_dir}/${name}" "${BDIR}/${name}"
				done
		else
				BDIR="${HOME}/.local/bin"
				for name in "$@"; do
						rm -f "${BDIR}/${name}"
						install "${src_dir}/${name}" "${BDIR}/${name}"
				done
		fi
}

# _gup_extract_tarball archive dest_dir [strip_components]
# Extracts a tarball to the destination directory.
_gup_extract_tarball() {
		local archive="$1"
		local dest="$2"
		local strip="${3:-1}"
		mkdir -p "${dest}"
		tar -axf "${archive}" -C "${dest}" --strip-components="${strip}"
}

# _gup_extract_zip archive dest_dir
# Extracts a zip to the destination directory.
_gup_extract_zip() {
		local archive="$1"
		local dest="$2"
		mkdir -p "${dest}"
		unzip -o "${archive}" -d "${dest}"
}

# :command.command_functions

# :command.function
gup_install_gup_command() {

	# src/install_gup_command.sh
	REPO="https://raw.githubusercontent.com/ppreeper/gup"

	if [ "$(id -u)" = 0 ]; then
			IDIR="/usr/local/bin"
			sudo mkdir -p "${IDIR}"
			sudo wget -q "${REPO}/main/gup" -O "${IDIR}/gup"
			chmod +x "${IDIR}/gup"
	else
			IDIR="${HOME}/.local/bin"
			mkdir -p "${IDIR}"
			wget -q "${REPO}/main/gup" -O "${IDIR}/gup"
			chmod +x "${IDIR}/gup"
	fi

}

# :command.function
gup_install_update_command() {

	# src/install_update_command.sh
	if [ "$(id -u)" = 0 ]; then
			IDIR="/usr/local/bin"
			sudo mkdir -p "${IDIR}"
	else
			IDIR="${HOME}/.local/bin"
			mkdir -p "${IDIR}"
	fi

	cat <<-_EOF_ > "${IDIR}/update"
#!/bin/bash
apt update -y && apt full-upgrade -y && apt autoremove -y && apt autoclean -y
_EOF_

	chmod +x "${IDIR}/update"

}

# :command.function
gup_install_go_command() {

	# src/install_go_command.sh
	APP="go"
	REPO="https://go.googlesource.com/go"
	DLREPO="https://dl.google.com/go"
	IDIR=/usr/local/lib
	BDIR=/usr/local/bin

	if [ "$(id -u)" != 0 ]; then
			echo "Error: ${APP} requires root privileges for system-wide install" >&2
			exit 1
	fi

	vers=$(git ls-remote --tags "${REPO}" | grep -E 'refs/tags/go[0-9]+\.[0-9]+(\.[0-9]+)?$' | awk '{print $2}' | sed 's/refs\/tags\///' | sort -V | uniq | tail -1)

	download() {
			echo "download $1 version"
			echo "installing $vers"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			FN="${vers}.linux-amd64.tar.gz"
			gup_download "${DLREPO}/${FN}" "${tmp_dir}/${FN}"
			rm -rf "${IDIR}/go"
			tar axf "${tmp_dir}/${FN}" -C "${IDIR}"
			find "${IDIR}/go/bin" -type f -exec sh -c 'ln -sf "$1" "${BDIR}/$(basename "$1")"' _ {} +
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | awk '{print $3}')
			if [ "${APPVER}" = "${vers}" ]; then
					echo "${APP} version is current"
			else
					download "${vers}"
			fi
	fi

}

# :command.function
gup_install_tinygo_command() {

	# src/install_tinygo_command.sh
	APP="tinygo"
	REPO="tinygo-org/tinygo"
	gup_fetch_release "${REPO}" '(contains("sha256") | not) and contains("linux-amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			if [ "$(id -u)" = 0 ]; then
					BDIR="/usr/local/bin"
					IDIR="/usr/local/lib"
					sudo rm -rf "${IDIR}/tinygo"
					sudo mkdir -p "${IDIR}/tinygo"
					_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${IDIR}/tinygo"
					find "${IDIR}/tinygo/bin/" -type f -exec sh -c '
							sudo rm -f "${BDIR}/$(basename "$1")"
							sudo ln -s "$1" "${BDIR}/$(basename "$1")"
					' _ {} +
			else
					BDIR="${HOME}/.local/bin"
					IDIR="${HOME}/.local/share"
					rm -rf "${IDIR}/tinygo"
					mkdir -p "${IDIR}/tinygo"
					_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${IDIR}/tinygo"
					find "${IDIR}/tinygo/bin/" -type f -exec sh -c '
							rm -f "${BDIR}/$(basename "$1")"
							ln -s "$1" "${BDIR}/$(basename "$1")"
					' _ {} +
			fi
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | awk '{print $3}')
			if [ "v${APPVER}" = "v${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
					exit 0
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_nodejs_command() {

	# src/install_nodejs_command.sh
	target="linux-x64.tar.xz"
	node_version="$(wget -qO- https://nodejs.org/download/release/index.tab | awk '{print $10,$1}' | grep -v "^-" | awk '{print $2}' | grep -v "^version" | sort -V | tail -1)"
	node_uri="https://nodejs.org/dist/${node_version}/node-${node_version}-${target}"

	echo "installing ${node_version}"

	tmp_dir=$(gup_mktemp_dir)
	trap 'rm -rf "${tmp_dir}"' RETURN

	gup_download "${node_uri}" "${tmp_dir}/node-${target}"

	if [ "$(id -u)" = 0 ]; then
			IDIR="/usr/local/share/node"
			BDIR="/usr/local/bin"
			sudo rm -rf "${IDIR}"
			sudo mkdir -p "${IDIR}"
			sudo tar axf "${tmp_dir}/node-${target}" --strip-components=1 -C "${IDIR}"
			sudo rm -f "${BDIR}/node"
			sudo ln -s "${IDIR}/bin/node" "${BDIR}/node"
			sudo rm -f "${BDIR}/npm"
			sudo ln -s "${IDIR}/bin/npm" "${BDIR}/npm"
			sudo rm -f "${BDIR}/npx"
			sudo ln -s "${IDIR}/bin/npx" "${BDIR}/npx"
			sudo rm -f "${BDIR}/corepack"
			sudo ln -s "${IDIR}/bin/corepack" "${BDIR}/corepack"
	else
			IDIR="${HOME}/.local/share/node"
			BDIR="${HOME}/.local/bin"
			mkdir -p "${BDIR}"
			rm -rf "${IDIR}"
			mkdir -p "${IDIR}"
			tar axf "${tmp_dir}/node-${target}" --strip-components=1 -C "${IDIR}"
			rm -f "${BDIR}/node"
			ln -s "${IDIR}/bin/node" "${BDIR}/node"
			rm -f "${BDIR}/npm"
			ln -s "${IDIR}/bin/npm" "${BDIR}/npm"
			rm -f "${BDIR}/npx"
			ln -s "${IDIR}/bin/npx" "${BDIR}/npx"
			rm -f "${BDIR}/corepack"
			ln -s "${IDIR}/bin/corepack" "${BDIR}/corepack"
	fi

}

# :command.function
gup_install_nvm_command() {

	# src/install_nvm_command.sh
	REPO="nvm-sh/nvm"
	vers=$(gup_get_latest_release "${REPO}")

	DLREPO="https://raw.githubusercontent.com/nvm-sh/nvm"

	tmp_dir=$(gup_mktemp_dir)
	trap 'rm -rf "${tmp_dir}"' RETURN

	gup_download "${DLREPO}/${vers}/install.sh" "${tmp_dir}/install.sh"
	bash "${tmp_dir}/install.sh"

}

# :command.function
gup_install_deno_command() {

	# src/install_deno_command.sh
	APP="deno"
	target="x86_64-unknown-linux-gnu"

	deno_version="$(wget -qO- https://dl.deno.land/release-latest.txt)"
	deno_uri="https://dl.deno.land/release/${deno_version}/deno-${target}.zip"

	echo "installing ${deno_version}"

	tmp_dir=$(gup_mktemp_dir)
	trap 'rm -rf "${tmp_dir}"' RETURN

	gup_download "${deno_uri}" "${tmp_dir}/deno.zip"
	unzip -d "${tmp_dir}" -o "${tmp_dir}/deno.zip"
	chmod +x "${tmp_dir}/deno"
	_gup_install_binary "${tmp_dir}/deno"

}

# :command.function
gup_install_bun_command() {

	# src/install_bun_command.sh
	APP="bun"
	REPO="oven-sh/bun"
	BDIR="${HOME}/.bun"
	target="linux-x64"
	gup_fetch_release "${REPO}" 'contains("linux-x64.zip")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			if [ "$(id -u)" = 0 ]; then
					BDIR="/usr/local"
					sudo mkdir -p "${BDIR}/bin"
					grep "BUN_INSTALL=${BDIR}" /etc/environment >/dev/null || echo "BUN_INSTALL=${BDIR}" | sudo tee -a /etc/environment
					grep "export BUN_INSTALL" /etc/profile >/dev/null || echo "export BUN_INSTALL=${BDIR}" | sudo tee -a /etc/profile
					grep 'export PATH=$BUN_INSTALL/bin:$PATH' /etc/profile >/dev/null || echo 'export PATH=$BUN_INSTALL/bin:$PATH' | sudo tee -a /etc/profile
			else
					BDIR="${HOME}/.bun"
					mkdir -p "${BDIR}/bin"
					grep "export BUN_INSTALL" "${HOME}/.bashrc" >/dev/null || echo "export BUN_INSTALL=${BDIR}" >>"${HOME}/.bashrc"
					grep 'export PATH=$BUN_INSTALL/bin:$PATH' "${HOME}/.bashrc" >/dev/null || echo 'export PATH=$BUN_INSTALL/bin:$PATH' >>"${HOME}/.bashrc"
			fi

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_zip "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}"
			if [ "$(id -u)" = 0 ]; then
					sudo install "${tmp_dir}/${APP}-${target}/${APP}" "${BDIR}/bin/${APP}"
			else
					install "${tmp_dir}/${APP}-${target}/${APP}" "${BDIR}/bin/${APP}"
			fi
			ln -f -s "${BDIR}/bin/bun" "${BDIR}/bin/bunx"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1)
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_uv_command() {

	# src/install_uv_command.sh
	APP="uv"
	REPO="astral-sh/uv"
	gup_fetch_release "${REPO}" '(contains("sha256") | not) and contains("x86_64-unknown-linux-gnu.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binaries "${tmp_dir}/${APP}" "${APP}" "${APP}x"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep "^${APP}" | awk '{print $2}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_rust_command() {

	# src/install_rust_command.sh
	APPBIN=$(command -v rustup 2>/dev/null || echo "")

	if [ -z "${APPBIN}" ]; then
			echo "Installing Rust"

			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "https://sh.rustup.rs" "${tmp_dir}/rustup-init.sh"
			sh "${tmp_dir}/rustup-init.sh" -y
	else
			echo "Upgrading Rust"
			"${APPBIN}" update
			exit 0
	fi

}

# :command.function
gup_install_zig_command() {

	# src/install_zig_command.sh
	APP="zig"
	IDIR=/usr/local/lib
	BDIR=/usr/local/bin

	if [ "$(id -u)" != 0 ]; then
			echo "Error: ${APP} requires root privileges for system-wide install" >&2
			exit 1
	fi

	vers=$(wget -qO- https://ziglang.org/download/index.json | jq 'to_entries | map(select(.key !="master")) | .[] | .key' | tr -d '"' | sort -V | uniq | tail -1)
	qstring=".\"${vers}\".\"x86_64-linux\".\"tarball\""
	DL=$(wget -qO- https://ziglang.org/download/index.json | jq "${qstring}" | tr -d '"')
	FN=$(basename "${DL}")

	download() {
			echo "download $1 version"
			echo "installing $vers"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${DL}" "${tmp_dir}/${FN}"
			rm -f "${BDIR}/zig"
			rm -rf "${IDIR}/zig"
			mkdir -p "${IDIR}/zig"
			tar axf "${tmp_dir}/${FN}" -C "${IDIR}/zig" --strip-components=1
			ln -sf "${IDIR}/zig/zig" "${BDIR}/zig"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1)
			if [ "${APPVER}" = "${vers}" ]; then
					echo "${APP} version is current"
			else
					download "${vers}"
			fi
	fi

}

# :command.function
gup_install_go_blueprint_command() {

	# src/install_go_blueprint_command.sh
	APP="go-blueprint"
	REPO="melkeydev/go-blueprint"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/melkeydev/go-blueprint@${vers}
}

# :command.function
gup_install_gowebly_command() {

	# src/install_gowebly_command.sh
	APP="gowebly"
	REPO="gowebly/gowebly"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/gowebly/gowebly/v2@${vers}
}

# :command.function
gup_install_goplay_command() {

	# src/install_goplay_command.sh
	APP="goplay"
	REPO="haya14busa/goplay"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/haya14busa/goplay/cmd/goplay@${vers}

}

# :command.function
gup_install_gotests_command() {

	# src/install_gotests_command.sh
	APP="gotests"
	REPO="cweill/gotests"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/cweill/gotests/gotests@${vers}

}

# :command.function
gup_install_impl_command() {

	# src/install_impl_command.sh
	APP="impl"
	REPO="josharian/impl"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/josharian/impl@${vers}

}

# :command.function
gup_install_gomodifytags_command() {

	# src/install_gomodifytags_command.sh
	APP="gomodifytags"
	REPO="fatih/gomodifytags"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/fatih/gomodifytags@${vers}

}

# :command.function
gup_install_air_command() {

	# src/install_air_command.sh
	APP="air"
	REPO="cosmtrek/air"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/cosmtrek/air@${vers}

}

# :command.function
gup_install_gofumpt_command() {

	# src/install_gofumpt_command.sh
	APP="gofumpt"
	REPO="mvdan/gofumpt"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install mvdan.cc/gofumpt@${vers}
}

# :command.function
gup_install_goimports_command() {

	# src/install_goimports_command.sh
	APP="goimports"
	REPO="golang/tools"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install golang.org/x/tools/cmd/goimports@${vers}
}

# :command.function
gup_install_golangci_lint_command() {

	# src/install_golangci_lint_command.sh
	APP="golangci-lint"
	REPO="golangci/golangci-lint"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@${vers}
}

# :command.function
gup_install_staticcheck_command() {

	# src/install_staticcheck_command.sh
	APP="staticcheck"
	REPO="dominikh/go-tools"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install honnef.co/go/tools/cmd/staticcheck@${vers}

}

# :command.function
gup_install_nilaway_command() {

	# src/install_nilaway_command.sh
	APP="nilaway"
	REPO="uber-go/nilaway"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install go.uber.org/nilaway/cmd/nilaway@${vers}
}

# :command.function
gup_install_swag_command() {

	# src/install_swag_command.sh
	APP="swag"
	REPO="swaggo/swag"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/swaggo/swag/cmd/swag@${vers}
}

# :command.function
gup_install_templ_command() {

	# src/install_templ_command.sh
	APP="templ"
	REPO="a-h/templ"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/a-h/templ/cmd/templ@${vers}

}

# :command.function
gup_install_ruff_command() {

	# src/install_ruff_command.sh
	APP="ruff"
	REPO="astral-sh/ruff"
	gup_fetch_release "${REPO}" '(contains("sha256") | not) and contains("x86_64-unknown-linux-gnu.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep "^${APP}" | awk '{print $2}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_scc_command() {

	# src/install_scc_command.sh
	APP="scc"
	REPO="boyter/scc"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/boyter/scc/v3@${vers}
}

# :command.function
gup_install_yamlfmt_command() {

	# src/install_yamlfmt_command.sh
	APP="yamlfmt"
	REPO="google/yamlfmt"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/google/yamlfmt/cmd/yamlfmt@${vers}
}

# :command.function
gup_install_gopls_command() {

	# src/install_gopls_command.sh
	APP="gopls"
	REPO="golang/tools"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install golang.org/x/tools/gopls@${vers}

}

# :command.function
gup_install_dlv_command() {

	# src/install_dlv_command.sh
	APP="dlv"
	REPO="go-delve/delve"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/go-delve/delve/cmd/dlv@${vers}

}

# :command.function
gup_install_zls_command() {

	# src/install_zls_command.sh
	APP="zls"
	REPO="zigtools/zls"
	gup_fetch_release "${REPO}" '(contains("minisig") | not) and contains("x86_64-linux")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/zls"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1)
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_neovim_command() {

	# src/install_neovim_command.sh
	APP="nvim"
	REPO="neovim/neovim"
	IDIR="${HOME}/.local/nvim"
	BDIR="${HOME}/.local/bin"
	gup_fetch_release "${REPO}" 'contains("linux-x86_64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			rm -rf "${IDIR:?}"
			mkdir -p "${IDIR}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${IDIR}"
			ln -sf "${IDIR}/bin/nvim" "${BDIR}/nvim"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep -i "^${APP}" | awk '{print $2}' | tr -d 'v')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_helix_command() {

	# src/install_helix_command.sh
	APP="hx"
	REPO="helix-editor/helix"
	IDIR="${HOME}/.config/helix"
	BDIR="${HOME}/.local/helix"
	gup_fetch_release "${REPO}" 'contains("x86_64-linux")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			mkdir -p "${IDIR}" "${BDIR}"
			touch "${IDIR}/config.toml"
			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			rm -rf "${BDIR:?}"
			mkdir -p "${BDIR}"
			tar axf "${tmp_dir}/${GUP_REL_FN}" --strip-components=1 -C "${BDIR}"
			ln -sf "${BDIR}/hx" "${HOME}/.local/bin/hx"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") -V 2>&1 | grep -e "^helix" | awk '{print $2}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_prometheus_command() {

	# src/install_prometheus_command.sh
	APP="prometheus"
	REPO="prometheus/prometheus"
	gup_fetch_release "${REPO}" 'contains("linux-amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binaries "${tmp_dir}/${APP}" "${APP}" promtool
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep -i "^${APP}" | awk '{print $3}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_grafana_command() {

	# src/install_grafana_command.sh
	if [ "$(id -u)" != 0 ]; then
			echo "Error: grafana requires root privileges for apt-based install" >&2
			exit 1
	fi

	wget -q https://packages.grafana.com/gpg.key -O /etc/apt/trusted.gpg.d/grafana.gpg.asc
	echo "deb https://packages.grafana.com/oss/deb stable main" | tee /etc/apt/sources.list.d/grafana.list
	apt update -y && apt install -y grafana && systemctl enable grafana-server

}

# :command.function
gup_install_loki_command() {

	# src/install_loki_command.sh
	APP="loki"
	REPO="grafana/loki"
	gup_fetch_release "${REPO}" 'contains("loki-linux-amd64")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_zip "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}-linux-amd64" "${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep -i "^${APP}" | awk '{print $3}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_node_exporter_command() {

	# src/install_node_exporter_command.sh
	APP="node_exporter"
	REPO="prometheus/node_exporter"
	gup_fetch_release "${REPO}" 'contains("linux-amd64")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep -i "^${APP}" | awk '{print $3}' | tr -d 'v')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_postgres_exporter_command() {

	# src/install_postgres_exporter_command.sh
	APP="postgres_exporter"
	REPO="prometheus-community/postgres_exporter"
	gup_fetch_release "${REPO}" 'contains("linux-amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep -i "^${APP}" | awk '{print $3}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_blackbox_exporter_command() {

	# src/install_blackbox_exporter_command.sh
	APP="blackbox_exporter"
	REPO="prometheus/blackbox_exporter"
	gup_fetch_release "${REPO}" '(contains("sha256") | not) and contains("linux-amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep -i "^${APP}" | awk '{print $3}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_snmp_exporter_command() {

	# src/install_snmp_exporter_command.sh
	APP="snmp_exporter"
	REPO="prometheus/${APP}"
	gup_fetch_release "${REPO}" 'contains("linux-amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep -i "^${APP}" | awk '{print $3}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_btop_command() {

	# src/install_btop_command.sh
	APP="btop"
	REPO="aristocratos/btop"
	gup_fetch_release "${REPO}" 'contains("x86_64") and contains(".tbz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"

			if [ "$(id -u)" = 0 ]; then
					PREFIX="/usr/local"
			else
					PREFIX="${HOME}/.local"
			fi

			# required directories
			mkdir -p "${PREFIX}/bin"
			mkdir -p "${PREFIX}/share/${APP}"
			mkdir -p "${PREFIX}/share/${APP}/themes"
			mkdir -p "${PREFIX}/share/applications"
			mkdir -p "${PREFIX}/share/icons/hicolor/48x48/apps"
			mkdir -p "${PREFIX}/share/icons/hicolor/scalable/apps"

			# btop binary
			install "${tmp_dir}/btop/bin/btop" "${PREFIX}/bin/btop"

			# doc
			cp "${tmp_dir}/btop/README.md" "${PREFIX}/share/${APP}/README.md"

			# Themes
			cp "${tmp_dir}/btop/themes/"* "${PREFIX}/share/${APP}/themes/"

			# desktop file
			cp "${tmp_dir}/btop/btop.desktop" "${PREFIX}/share/applications/btop.desktop"

			# icons
			cp "${tmp_dir}/btop/Img/icon.png" "${PREFIX}/share/icons/hicolor/48x48/apps/btop.png"
			cp "${tmp_dir}/btop/Img/icon.svg" "${PREFIX}/share/icons/hicolor/scalable/apps/btop.svg"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_gotop_command() {

	# src/install_gotop_command.sh
	APP="gotop"
	REPO="xxxserxxx/gotop"
	gup_fetch_release "${REPO}" 'contains("linux_amd64.tgz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			tar axf "${tmp_dir}/${GUP_REL_FN}" -C "${tmp_dir}" "${APP}"
			_gup_install_binary "${tmp_dir}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep "^${APP}" | awk '{print $2}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_lazydocker_command() {

	# src/install_lazydocker_command.sh
	APP="lazydocker"
	REPO="jesseduffield/lazydocker"
	gup_fetch_release "${REPO}" 'contains("linux_x86_64")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			tar axf "${tmp_dir}/${GUP_REL_FN}" -C "${tmp_dir}" "${APP}"
			_gup_install_binary "${tmp_dir}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | awk '{print $6}' | sed 's/,//g' | awk -F'=' '{print $2}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_minikube_command() {

	# src/install_minikube_command.sh
	APP="minikube"
	DL="https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64"

	echo "installing latest ${APP}"

	tmp_dir=$(gup_mktemp_dir)
	trap 'rm -rf "${tmp_dir}"' RETURN

	gup_download "${DL}" "${tmp_dir}/${APP}"
	chmod +x "${tmp_dir}/${APP}"
	_gup_install_binary "${tmp_dir}/${APP}"

}

# :command.function
gup_install_kubectl_command() {

	# src/install_kubectl_command.sh
	APP="kubectl"
	STABLE_URL="https://storage.googleapis.com/kubernetes-release/release/stable.txt"
	BASE_URL="https://storage.googleapis.com/kubernetes-release/release"

	tmp_dir=$(gup_mktemp_dir)
	trap 'rm -rf "${tmp_dir}"' RETURN

	gup_download "${STABLE_URL}" "${tmp_dir}/stable.txt"
	stable=$(cat "${tmp_dir}/stable.txt")
	gup_download "${BASE_URL}/${stable}/bin/linux/amd64/kubectl" "${tmp_dir}/${APP}"
	chmod +x "${tmp_dir}/${APP}"
	_gup_install_binary "${tmp_dir}/${APP}"

}

# :command.function
gup_install_k9s_command() {

	# src/install_k9s_command.sh
	APP="k9s"
	REPO="derailed/k9s"
	gup_fetch_release "${REPO}" '(contains("json") | not) and contains("Linux") and contains("amd64")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			tar axf "${tmp_dir}/${GUP_REL_FN}" -C "${tmp_dir}" "${APP}"
			_gup_install_binary "${tmp_dir}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | grep Version | awk '{print $2}' | tr -d 'v')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_etcd_command() {

	# src/install_etcd_command.sh
	APP="etcd"
	REPO="etcd-io/etcd"
	gup_fetch_release "${REPO}" 'contains("linux-amd64")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/etcd"

			if [ "$(id -u)" = 0 ]; then
					BDIR="/usr/local/bin"
			else
					BDIR="${HOME}/.local/bin"
					mkdir -p "${BDIR}"
			fi

			install "${tmp_dir}/etcd/etcd" "${BDIR}/etcd"
			install "${tmp_dir}/etcd/etcdctl" "${BDIR}/etcdctl"
			install "${tmp_dir}/etcd/etcdutl" "${BDIR}/etcdutl"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep "etcd.*Version" | awk '{print $3}' | sed 's/^v//')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_vagrant_command() {

	# src/install_vagrant_command.sh
	if [ "$(id -u)" != 0 ]; then
			echo "Error: vagrant requires root privileges for apt-based install" >&2
			exit 1
	fi

	lsb=$(grep VERSION_CODENAME /etc/os-release | awk -F'=' '{print $2}')
	wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
	echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com ${lsb} main" | tee /etc/apt/sources.list.d/hashicorp.list
	apt update -y && apt install -y vagrant

}

# :command.function
gup_install_doctl_command() {

	# src/install_doctl_command.sh
	APP="doctl"
	REPO="digitalocean/doctl"
	gup_fetch_release "${REPO}" 'contains("linux-amd64")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			if [ "$(id -u)" = 0 ]; then
					BDIR="/usr/local/bin"
					sudo tar -axf "${tmp_dir}/${GUP_REL_FN}" -C "${BDIR}" "${APP}"
					sudo chmod +x "${BDIR}/${APP}"
			else
					BDIR="${HOME}/.local/bin"
					tar -axf "${tmp_dir}/${GUP_REL_FN}" -C "${BDIR}" "${APP}"
					chmod +x "${BDIR}/${APP}"
			fi
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | grep "doctl version" | awk '{print $3}' | awk -F'-' '{print $1}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_b2_command() {

	# src/install_b2_command.sh
	APP="b2"
	DL="https://github.com/Backblaze/B2_Command_Line_Tool/releases/latest/download/b2-linux"

	download() {
			echo "download ${APP} ..."

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${DL}" "${tmp_dir}/${APP}"
			chmod +x "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}"
	}

	download "${APP}"

}

# :command.function
gup_install_githubcli_command() {

	# src/install_githubcli_command.sh
	APP="gh"
	REPO="cli/cli"
	gup_fetch_release "${REPO}" 'contains("linux_amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"

			if [ "$(id -u)" = 0 ]; then
					IDIR="/usr/local/lib"
					BDIR="/usr/local/bin"
					rm -rf "${IDIR}/${APP}"
					mkdir -p "${IDIR}/${APP}"
					tar -axf "${tmp_dir}/${GUP_REL_FN}" --strip-components=1 -C "${IDIR}/${APP}"
					find "${IDIR}/${APP}/bin" -type f -exec sh -c 'ln -sf "$1" "${BDIR}/$(basename "$1")"' _ {} +
			else
					IDIR="${HOME}/.local/lib"
					BDIR="${HOME}/.local/bin"
					rm -rf "${IDIR}/${APP}"
					mkdir -p "${IDIR}/${APP}"
					tar -axf "${tmp_dir}/${GUP_REL_FN}" --strip-components=1 -C "${IDIR}/${APP}"
					find "${IDIR}/${APP}/bin" -type f -exec sh -c 'ln -sf "$1" "${BDIR}/$(basename "$1")"' _ {} +
			fi
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | awk '{print $3}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_forgejocli_command() {

	# src/install_forgejocli_command.sh
	APP="fj"
	DL="https://codeberg.org/forgejo-contrib/forgejo-cli/releases/download/latest/forgejo-cli-linux.tar.gz"
	FN="forgejo-cli-linux.tar.gz"

	download() {
			echo "download $1 version"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${DL}" "${tmp_dir}/${FN}"
			tar -axf "${tmp_dir}/${FN}" -C "${tmp_dir}"

			if [ "$(id -u)" = 0 ]; then
					BDIR="/usr/local/bin"
			else
					BDIR="${HOME}/.local/bin"
					mkdir -p "${BDIR}"
			fi

			install -Dm755 "${tmp_dir}/${APP}" "${BDIR}/${APP}"
	}

	download new

}

# :command.function
gup_install_caddy2_command() {

	# src/install_caddy2_command.sh
	APP="caddy"
	REPO="caddyserver/caddy"
	gup_fetch_release "${REPO}" '(contains(".sig") | not) and contains("linux_amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | grep -i "^v" | awk '{print $1}')
			if [ "${APPVER}" = "v${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_traefik_command() {

	# src/install_traefik_command.sh
	APP="traefik"
	REPO="traefik/traefik"
	gup_fetch_release "${REPO}" 'contains("linux_amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | grep ^Version | awk '{print $2}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_hugo_command() {

	# src/install_hugo_command.sh
	APP="hugo"
	REPO="gohugoio/hugo"
	gup_fetch_release "${REPO}" 'contains("extended") and contains("linux-amd64.tar.gz") and (contains("withdeploy") | not)'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			if [ "$(id -u)" = 0 ]; then
					BDIR="/usr/local/bin"
					sudo tar -axf "${tmp_dir}/${GUP_REL_FN}" -C "${BDIR}" "${APP}"
			else
					BDIR="${HOME}/.local/bin"
					tar -axf "${tmp_dir}/${GUP_REL_FN}" -C "${BDIR}" "${APP}"
			fi
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | awk '{print $2}' | awk -F'-' '{print $1}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_tailwindcss_command() {

	# src/install_tailwindcss_command.sh
	APP="tailwindcss"
	REPO="tailwindlabs/tailwindcss"
	gup_fetch_release "${REPO}" 'contains("linux-x64") and (contains("musl") | not)'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_install_binary "${tmp_dir}/${GUP_REL_FN}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --help 2>&1 | grep "${APP}.*v" | awk '{print $3}' | tr -d 'v')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_marp_command() {

	# src/install_marp_command.sh
	APP="marp"
	REPO="marp-team/marp-cli"
	gup_fetch_release "${REPO}" 'contains("linux")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			tar axf "${tmp_dir}/${GUP_REL_FN}" -C "${tmp_dir}" "${APP}"
			_gup_install_binary "${tmp_dir}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | grep marp-cli | awk '{print $2}' | tr -d 'v')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_duf_command() {

	# src/install_duf_command.sh
	APP="duf"
	REPO="muesli/duf"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/muesli/duf@${vers}
}

# :command.function
gup_install_gdu_command() {

	# src/install_gdu_command.sh
	APP="gdu"
	REPO="dundee/gdu"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/dundee/gdu/v5/cmd/gdu@${vers}
}

# :command.function
gup_install_gum_command() {

	# src/install_gum_command.sh
	APP="gum"
	REPO="charmbracelet/gum"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/charmbracelet/gum@${vers}
}

# :command.function
gup_install_lazygit_command() {

	# src/install_lazygit_command.sh
	APP="lazygit"
	REPO="jesseduffield/lazygit"
	gup_fetch_release "${REPO}" 'contains("linux_x86_64")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			tar axf "${tmp_dir}/${GUP_REL_FN}" -C "${tmp_dir}" "${APP}"
			_gup_install_binary "${tmp_dir}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | awk '{print $6}' | sed 's/,//g' | awk -F'=' '{print $2}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_muffet_command() {

	# src/install_muffet_command.sh
	APP="muffet"
	REPO="raviqqe/muffet"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/raviqqe/muffet/v2@${vers}
}

# :command.function
gup_install_s5cmd_command() {

	# src/install_s5cmd_command.sh
	APP="s5cmd"
	REPO="peak/s5cmd"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/peak/s5cmd/v2@${vers}

}

# :command.function
gup_install_sshconfig_command() {

	# src/install_sshconfig_command.sh
	APP="sshconfig"
	REPO="ppreeper/sshconfig"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/ppreeper/sshconfig@${vers}

}

# :command.function
gup_install_task_command() {

	# src/install_task_command.sh
	APP="task"
	REPO="go-task/task"
	gup_fetch_release "${REPO}" 'contains("linux_amd64.tar.gz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1 | tr -d 'v')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_timer_command() {

	# src/install_timer_command.sh
	APP="timer"
	REPO="caarlos0/timer"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/caarlos0/timer@${vers}
}

# :command.function
gup_install_ytdlp_command() {

	# src/install_ytdlp_command.sh
	APP="yt-dlp"
	REPO="yt-dlp/yt-dlp"
	gup_fetch_release "${REPO}" 'contains("_linux")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_install_binary "${tmp_dir}/${GUP_REL_FN}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") --version 2>&1)
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_zfsnap_command() {

	# src/install_zfsnap_command.sh
	APP="zfsnap"
	REPO="https://github.com/zfsnap/zfsnap"
	BREPO="https://raw.githubusercontent.com/zfsnap/zfsnap"
	vers=$(git ls-remote --tags "${REPO}" | grep "refs/tags/v2.*[0-9]$" | grep -v -e rc -e alpha | awk '{print $2}' | sed 's/refs\/tags\///' | sort -V | uniq | tail -1)

	echo "installing ${vers}"

	tmp_dir=$(gup_mktemp_dir)
	trap 'rm -rf "${tmp_dir}"' RETURN

	if [ "$(id -u)" = 0 ]; then
			SHARE_DIR="/usr/share/zfsnap"
			SBIN_DIR="/usr/sbin"
			DO_SUDO="sudo"
	else
			SHARE_DIR="${HOME}/.local/share/zfsnap"
			SBIN_DIR="${HOME}/.local/bin"
			DO_SUDO=""
	fi

	${DO_SUDO} mkdir -p "${SHARE_DIR}/commands"

	gup_download "${BREPO}/${vers}/share/zfsnap/core.sh" "${tmp_dir}/core.sh"
	gup_download "${BREPO}/${vers}/share/zfsnap/commands/destroy.sh" "${tmp_dir}/destroy.sh"
	gup_download "${BREPO}/${vers}/share/zfsnap/commands/recurseback.sh" "${tmp_dir}/recurseback.sh"
	gup_download "${BREPO}/${vers}/share/zfsnap/commands/snapshot.sh" "${tmp_dir}/snapshot.sh"
	gup_download "${BREPO}/${vers}/sbin/zfsnap.sh" "${tmp_dir}/zfsnap"

	${DO_SUDO} install -m 0755 "${tmp_dir}/core.sh" "${SHARE_DIR}/core.sh"
	${DO_SUDO} install -m 0755 "${tmp_dir}/destroy.sh" "${SHARE_DIR}/commands/destroy.sh"
	${DO_SUDO} install -m 0755 "${tmp_dir}/recurseback.sh" "${SHARE_DIR}/commands/recurseback.sh"
	${DO_SUDO} install -m 0755 "${tmp_dir}/snapshot.sh" "${SHARE_DIR}/commands/snapshot.sh"
	${DO_SUDO} install -m 0755 "${tmp_dir}/zfsnap" "${SBIN_DIR}/zfsnap"

}

# :command.function
gup_install_terraform_command() {

	# src/install_terraform_command.sh
	APP="terraform"
	REPO="hashicorp/terraform"
	gup_fetch_release "${REPO}" 'contains("linux_amd64.zip")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_zip "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") version 2>&1 | grep -i "^${APP}" | awk '{print $2}' | tr -d 'v')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_distrobuilder_command() {

	# src/install_distrobuilder_command.sh
	APP="distrobuilder"
	REPO="https://github.com/lxc/distrobuilder"

	if ! command -v make >/dev/null 2>&1; then
			echo "Error: 'make' is required to build distrobuilder" >&2
			exit 1
	fi

	tmp_dir=$(gup_mktemp_dir)
	trap 'rm -rf "${tmp_dir}"' RETURN

	echo "cloning and building ${APP}..."
	git clone "${REPO}" "${tmp_dir}/distrobuilder"
	(cd "${tmp_dir}/distrobuilder" && make)

	if [ "$(id -u)" = 0 ]; then
			BDIR="/usr/local/bin"
			sudo install -m 0755 "${tmp_dir}/distrobuilder/distrobuilder" "${BDIR}/distrobuilder"
	else
			BDIR="${HOME}/.local/bin"
			mkdir -p "${BDIR}"
			install -m 0755 "${tmp_dir}/distrobuilder/distrobuilder" "${BDIR}/distrobuilder"
	fi
	echo "installed ${APP} to ${BDIR}/distrobuilder"

}

# :command.function
gup_install_upx_command() {

	# src/install_upx_command.sh
	APP="upx"
	REPO="upx/upx"
	gup_fetch_release "${REPO}" '(contains("sha256") | not) and contains("amd64_linux.tar.xz")'

	download() {
			echo "download $1 version"
			echo "installing ${GUP_REL_VERSION}"

			local tmp_dir
			tmp_dir=$(gup_mktemp_dir)
			trap 'rm -rf "${tmp_dir}"' RETURN

			gup_download "${GUP_REL_DL}" "${tmp_dir}/${GUP_REL_FN}"
			_gup_extract_tarball "${tmp_dir}/${GUP_REL_FN}" "${tmp_dir}/${APP}"
			_gup_install_binary "${tmp_dir}/${APP}/${APP}"
	}

	if [ -z "$(command -v "${APP}")" ]; then
			download new
	else
			APPVER=$($(command -v "${APP}") -V 2>&1 | grep -e "^${APP}" | awk '{print $2}')
			if [ "${APPVER}" = "${GUP_REL_VERSION}" ]; then
					echo "${APP} version is current"
			else
					download "${GUP_REL_VERSION}"
			fi
	fi

}

# :command.function
gup_install_sqlc_command() {

	# src/install_sqlc_command.sh
	APP="sqlc"
	REPO="sqlc-dev/sqlc"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install github.com/sqlc-dev/sqlc/cmd/sqlc@${vers}
}

# :command.function
gup_install_mkcert_command() {

	# src/install_mkcert_command.sh
	APP="mkcert"

	echo "installing ${APP}"

	tmp_dir=$(gup_mktemp_dir)
	trap 'rm -rf "${tmp_dir}"' RETURN

	gup_download "https://dl.filippo.io/mkcert/latest?for=linux/amd64" "${tmp_dir}/${APP}"
	chmod +x "${tmp_dir}/${APP}"
	_gup_install_binary "${tmp_dir}/${APP}"

}

# :command.function
gup_install_govulncheck_command() {

	# src/install_govulncheck_command.sh
	APP="govulncheck"
	REPO="golang/vuln"
	vers=$(gup_get_latest_release "${REPO}")

	gup_ensure_go
	go install golang.org/x/vuln/cmd/govulncheck@${vers}

}

# :command.function
gup_install_nerdfonts_command() {

	# src/install_nerdfonts_command.sh
	REPO="ryanoasis/nerd-fonts"
	FONTDIR="${HOME}/.local/share/fonts"

	download() {
			echo "installing $1 font"
			local DL
			DL=$(echo "${GUP_REL_JSON}" | jq -r '.assets[] | select(.name | contains("tar.xz")) | .browser_download_url' | grep "$1")
			for font in ${DL}; do
					local tmp_dir
					tmp_dir=$(gup_mktemp_dir)
					trap 'rm -rf "${tmp_dir}"' RETURN

					gup_download "${font}" "${tmp_dir}/font.tar.xz"
					mkdir -p "${FONTDIR}"
					tar -xf "${tmp_dir}/font.tar.xz" -C "${FONTDIR}" --wildcards "*ttf"
			done
	}

	gup_fetch_release "${REPO}" 'contains("tar.xz")'
	fonts="${args[font]}"
	for font in ${fonts}; do
		download "$font"
	done

}

# :command.function
gup_remove_neovim_command() {

	# src/remove_neovim_command.sh
	rm -rf "${HOME}/.local/nvim" "${HOME}/.local/share/nvim" "${HOME}/.local/state/nvim" "${HOME}/.cache/nvim" "${HOME}/.config/nvim" "${HOME}/.local/bin/nvim"

}

# :command.function
gup_systemd_loki_command() {

	# src/systemd_loki_command.sh
	function servicefile() {
	cat <<-_EOF_ | sudo tee /etc/systemd/system/loki.service > /dev/null
[Unit]
Description=Loki service
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/loki -config.file /etc/loki/loki-config.yml

[Install]
WantedBy=multi-user.target
_EOF_
	}

	servicefile
	sudo systemctl daemon-reload
}

# :command.function
gup_systemd_promtail_command() {

	# src/systemd_promtail_command.sh
	function servicefile() {
	cat <<-_EOF_ | sudo tee /etc/systemd/system/promtail.service > /dev/null
[Unit]
Description=Promtail service
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/promtail -config.file /etc/promtail/promtail-config.yml

[Install]
WantedBy=multi-user.target
_EOF_
	}

	servicefile
	sudo systemctl daemon-reload
}

# :command.parse_requirements
parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--version | -v)
				version_command
				exit
				;;

			--help | -h)
				long_usage=yes
				gup_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action=${1:-}

	case $action in
		-*) ;;

		install)
			action="install"
			shift
			gup_install_parse_requirements "$@"
			shift $#
			;;

		remove)
			action="remove"
			shift
			gup_remove_parse_requirements "$@"
			shift $#
			;;

		systemd)
			action="systemd"
			shift
			gup_systemd_parse_requirements "$@"
			shift $#
			;;

		# :command.command_fallback
		"")
			gup_usage >&2
			exit 1
			;;

		*)
			printf "invalid command: %s\n" "$action" >&2
			exit 1
			;;

	esac

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action=${1:-}

	case $action in
		-*) ;;

		gup)
			action="gup"
			shift
			gup_install_gup_parse_requirements "$@"
			shift $#
			;;

		update)
			action="update"
			shift
			gup_install_update_parse_requirements "$@"
			shift $#
			;;

		go)
			action="go"
			shift
			gup_install_go_parse_requirements "$@"
			shift $#
			;;

		tinygo)
			action="tinygo"
			shift
			gup_install_tinygo_parse_requirements "$@"
			shift $#
			;;

		nodejs)
			action="nodejs"
			shift
			gup_install_nodejs_parse_requirements "$@"
			shift $#
			;;

		nvm)
			action="nvm"
			shift
			gup_install_nvm_parse_requirements "$@"
			shift $#
			;;

		deno)
			action="deno"
			shift
			gup_install_deno_parse_requirements "$@"
			shift $#
			;;

		bun)
			action="bun"
			shift
			gup_install_bun_parse_requirements "$@"
			shift $#
			;;

		uv)
			action="uv"
			shift
			gup_install_uv_parse_requirements "$@"
			shift $#
			;;

		rust)
			action="rust"
			shift
			gup_install_rust_parse_requirements "$@"
			shift $#
			;;

		zig)
			action="zig"
			shift
			gup_install_zig_parse_requirements "$@"
			shift $#
			;;

		go-blueprint)
			action="go-blueprint"
			shift
			gup_install_go_blueprint_parse_requirements "$@"
			shift $#
			;;

		gowebly)
			action="gowebly"
			shift
			gup_install_gowebly_parse_requirements "$@"
			shift $#
			;;

		goplay)
			action="goplay"
			shift
			gup_install_goplay_parse_requirements "$@"
			shift $#
			;;

		gotests)
			action="gotests"
			shift
			gup_install_gotests_parse_requirements "$@"
			shift $#
			;;

		impl)
			action="impl"
			shift
			gup_install_impl_parse_requirements "$@"
			shift $#
			;;

		gomodifytags)
			action="gomodifytags"
			shift
			gup_install_gomodifytags_parse_requirements "$@"
			shift $#
			;;

		air)
			action="air"
			shift
			gup_install_air_parse_requirements "$@"
			shift $#
			;;

		gofumpt)
			action="gofumpt"
			shift
			gup_install_gofumpt_parse_requirements "$@"
			shift $#
			;;

		goimports)
			action="goimports"
			shift
			gup_install_goimports_parse_requirements "$@"
			shift $#
			;;

		golangci-lint)
			action="golangci-lint"
			shift
			gup_install_golangci_lint_parse_requirements "$@"
			shift $#
			;;

		staticcheck)
			action="staticcheck"
			shift
			gup_install_staticcheck_parse_requirements "$@"
			shift $#
			;;

		nilaway)
			action="nilaway"
			shift
			gup_install_nilaway_parse_requirements "$@"
			shift $#
			;;

		swag)
			action="swag"
			shift
			gup_install_swag_parse_requirements "$@"
			shift $#
			;;

		templ)
			action="templ"
			shift
			gup_install_templ_parse_requirements "$@"
			shift $#
			;;

		ruff)
			action="ruff"
			shift
			gup_install_ruff_parse_requirements "$@"
			shift $#
			;;

		scc)
			action="scc"
			shift
			gup_install_scc_parse_requirements "$@"
			shift $#
			;;

		yamlfmt)
			action="yamlfmt"
			shift
			gup_install_yamlfmt_parse_requirements "$@"
			shift $#
			;;

		gopls)
			action="gopls"
			shift
			gup_install_gopls_parse_requirements "$@"
			shift $#
			;;

		dlv)
			action="dlv"
			shift
			gup_install_dlv_parse_requirements "$@"
			shift $#
			;;

		zls)
			action="zls"
			shift
			gup_install_zls_parse_requirements "$@"
			shift $#
			;;

		neovim)
			action="neovim"
			shift
			gup_install_neovim_parse_requirements "$@"
			shift $#
			;;

		helix)
			action="helix"
			shift
			gup_install_helix_parse_requirements "$@"
			shift $#
			;;

		prometheus)
			action="prometheus"
			shift
			gup_install_prometheus_parse_requirements "$@"
			shift $#
			;;

		grafana)
			action="grafana"
			shift
			gup_install_grafana_parse_requirements "$@"
			shift $#
			;;

		loki)
			action="loki"
			shift
			gup_install_loki_parse_requirements "$@"
			shift $#
			;;

		node_exporter)
			action="node_exporter"
			shift
			gup_install_node_exporter_parse_requirements "$@"
			shift $#
			;;

		postgres_exporter)
			action="postgres_exporter"
			shift
			gup_install_postgres_exporter_parse_requirements "$@"
			shift $#
			;;

		blackbox_exporter)
			action="blackbox_exporter"
			shift
			gup_install_blackbox_exporter_parse_requirements "$@"
			shift $#
			;;

		snmp_exporter)
			action="snmp_exporter"
			shift
			gup_install_snmp_exporter_parse_requirements "$@"
			shift $#
			;;

		btop)
			action="btop"
			shift
			gup_install_btop_parse_requirements "$@"
			shift $#
			;;

		gotop)
			action="gotop"
			shift
			gup_install_gotop_parse_requirements "$@"
			shift $#
			;;

		lazydocker)
			action="lazydocker"
			shift
			gup_install_lazydocker_parse_requirements "$@"
			shift $#
			;;

		minikube)
			action="minikube"
			shift
			gup_install_minikube_parse_requirements "$@"
			shift $#
			;;

		kubectl)
			action="kubectl"
			shift
			gup_install_kubectl_parse_requirements "$@"
			shift $#
			;;

		k9s)
			action="k9s"
			shift
			gup_install_k9s_parse_requirements "$@"
			shift $#
			;;

		etcd)
			action="etcd"
			shift
			gup_install_etcd_parse_requirements "$@"
			shift $#
			;;

		vagrant)
			action="vagrant"
			shift
			gup_install_vagrant_parse_requirements "$@"
			shift $#
			;;

		doctl)
			action="doctl"
			shift
			gup_install_doctl_parse_requirements "$@"
			shift $#
			;;

		b2)
			action="b2"
			shift
			gup_install_b2_parse_requirements "$@"
			shift $#
			;;

		githubcli)
			action="githubcli"
			shift
			gup_install_githubcli_parse_requirements "$@"
			shift $#
			;;

		forgejocli)
			action="forgejocli"
			shift
			gup_install_forgejocli_parse_requirements "$@"
			shift $#
			;;

		caddy2)
			action="caddy2"
			shift
			gup_install_caddy2_parse_requirements "$@"
			shift $#
			;;

		traefik)
			action="traefik"
			shift
			gup_install_traefik_parse_requirements "$@"
			shift $#
			;;

		hugo)
			action="hugo"
			shift
			gup_install_hugo_parse_requirements "$@"
			shift $#
			;;

		tailwindcss)
			action="tailwindcss"
			shift
			gup_install_tailwindcss_parse_requirements "$@"
			shift $#
			;;

		marp)
			action="marp"
			shift
			gup_install_marp_parse_requirements "$@"
			shift $#
			;;

		duf)
			action="duf"
			shift
			gup_install_duf_parse_requirements "$@"
			shift $#
			;;

		gdu)
			action="gdu"
			shift
			gup_install_gdu_parse_requirements "$@"
			shift $#
			;;

		gum)
			action="gum"
			shift
			gup_install_gum_parse_requirements "$@"
			shift $#
			;;

		lazygit)
			action="lazygit"
			shift
			gup_install_lazygit_parse_requirements "$@"
			shift $#
			;;

		muffet)
			action="muffet"
			shift
			gup_install_muffet_parse_requirements "$@"
			shift $#
			;;

		s5cmd)
			action="s5cmd"
			shift
			gup_install_s5cmd_parse_requirements "$@"
			shift $#
			;;

		sshconfig)
			action="sshconfig"
			shift
			gup_install_sshconfig_parse_requirements "$@"
			shift $#
			;;

		task)
			action="task"
			shift
			gup_install_task_parse_requirements "$@"
			shift $#
			;;

		timer)
			action="timer"
			shift
			gup_install_timer_parse_requirements "$@"
			shift $#
			;;

		ytdlp)
			action="ytdlp"
			shift
			gup_install_ytdlp_parse_requirements "$@"
			shift $#
			;;

		zfsnap)
			action="zfsnap"
			shift
			gup_install_zfsnap_parse_requirements "$@"
			shift $#
			;;

		terraform)
			action="terraform"
			shift
			gup_install_terraform_parse_requirements "$@"
			shift $#
			;;

		distrobuilder)
			action="distrobuilder"
			shift
			gup_install_distrobuilder_parse_requirements "$@"
			shift $#
			;;

		upx)
			action="upx"
			shift
			gup_install_upx_parse_requirements "$@"
			shift $#
			;;

		sqlc)
			action="sqlc"
			shift
			gup_install_sqlc_parse_requirements "$@"
			shift $#
			;;

		mkcert)
			action="mkcert"
			shift
			gup_install_mkcert_parse_requirements "$@"
			shift $#
			;;

		govulncheck)
			action="govulncheck"
			shift
			gup_install_govulncheck_parse_requirements "$@"
			shift $#
			;;

		nerdfonts)
			action="nerdfonts"
			shift
			gup_install_nerdfonts_parse_requirements "$@"
			shift $#
			;;

		# :command.command_fallback
		"")
			gup_install_usage >&2
			exit 1
			;;

		*)
			printf "invalid command: %s\n" "$action" >&2
			exit 1
			;;

	esac

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gup_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gup_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gup"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_update_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_update_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install update"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_go_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_go_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install go"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_tinygo_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_tinygo_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install tinygo"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_nodejs_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_nodejs_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install nodejs"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_nvm_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_nvm_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install nvm"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_deno_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_deno_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install deno"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_bun_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_bun_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install bun"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_uv_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_uv_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install uv"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_rust_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_rust_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install rust"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_zig_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_zig_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install zig"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_go_blueprint_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_go_blueprint_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install go-blueprint"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gowebly_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gowebly_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gowebly"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_goplay_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_goplay_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install goplay"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gotests_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gotests_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gotests"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_impl_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_impl_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install impl"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gomodifytags_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gomodifytags_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gomodifytags"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_air_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_air_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install air"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gofumpt_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gofumpt_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gofumpt"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_goimports_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_goimports_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install goimports"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_golangci_lint_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_golangci_lint_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install golangci-lint"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_staticcheck_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_staticcheck_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install staticcheck"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_nilaway_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_nilaway_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install nilaway"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_swag_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_swag_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install swag"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_templ_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_templ_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install templ"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_ruff_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_ruff_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install ruff"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_scc_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_scc_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install scc"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_yamlfmt_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_yamlfmt_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install yamlfmt"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gopls_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gopls_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gopls"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_dlv_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_dlv_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install dlv"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_zls_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_zls_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install zls"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_neovim_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_neovim_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install neovim"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_helix_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_helix_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install helix"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_prometheus_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_prometheus_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install prometheus"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_grafana_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_grafana_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install grafana"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_loki_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_loki_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install loki"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_node_exporter_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_node_exporter_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install node_exporter"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_postgres_exporter_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_postgres_exporter_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install postgres_exporter"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_blackbox_exporter_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_blackbox_exporter_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install blackbox_exporter"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_snmp_exporter_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_snmp_exporter_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install snmp_exporter"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_btop_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_btop_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install btop"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gotop_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gotop_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gotop"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_lazydocker_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_lazydocker_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install lazydocker"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_minikube_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_minikube_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install minikube"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_kubectl_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_kubectl_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install kubectl"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_k9s_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_k9s_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install k9s"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_etcd_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_etcd_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install etcd"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_vagrant_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_vagrant_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install vagrant"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_doctl_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_doctl_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install doctl"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_b2_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_b2_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install b2"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_githubcli_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_githubcli_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install githubcli"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_forgejocli_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_forgejocli_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install forgejocli"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_caddy2_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_caddy2_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install caddy2"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_traefik_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_traefik_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install traefik"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_hugo_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_hugo_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install hugo"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_tailwindcss_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_tailwindcss_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install tailwindcss"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_marp_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_marp_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install marp"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_duf_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_duf_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install duf"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gdu_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gdu_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gdu"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_gum_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_gum_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install gum"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_lazygit_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_lazygit_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install lazygit"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_muffet_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_muffet_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install muffet"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_s5cmd_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_s5cmd_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install s5cmd"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_sshconfig_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_sshconfig_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install sshconfig"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_task_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_task_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install task"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_timer_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_timer_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install timer"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_ytdlp_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_ytdlp_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install ytdlp"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_zfsnap_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_zfsnap_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install zfsnap"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_terraform_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_terraform_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install terraform"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_distrobuilder_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_distrobuilder_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install distrobuilder"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_upx_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_upx_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install upx"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_sqlc_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_sqlc_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install sqlc"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_mkcert_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_mkcert_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install mkcert"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_govulncheck_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_govulncheck_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install govulncheck"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_install_nerdfonts_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_install_nerdfonts_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="install nerdfonts"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_repeatable
				# :argument.case_repeatable
				escaped="$(printf '%q' "$1")"
				if [[ -z ${args['font']+x} ]]; then
					args['font']="$escaped"
				else
					args['font']="${args['font']} $escaped"

				fi
				shift

				;;

		esac
	done

	# :command.required_args_filter
	if [[ -z ${args['font']+x} ]]; then
		printf "missing required argument: FONT\nusage: gup install nerdfonts FONT...\n" >&2

		exit 1
	fi

	# :command.whitelist_filter
	input_array=''
	eval "input_array=(${args[font]})"
	for i in "${input_array[@]}"; do
		if [[ ! $i =~ ^(Ubuntu|UbuntuMono|Hack|FiraMono|Noto)$ ]]; then
			printf "%s\n" "font must be one of: Ubuntu, UbuntuMono, Hack, FiraMono, Noto" >&2
			exit 1
		fi
	done

}

# :command.parse_requirements
gup_remove_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_remove_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action=${1:-}

	case $action in
		-*) ;;

		neovim)
			action="neovim"
			shift
			gup_remove_neovim_parse_requirements "$@"
			shift $#
			;;

		# :command.command_fallback
		"")
			gup_remove_usage >&2
			exit 1
			;;

		*)
			printf "invalid command: %s\n" "$action" >&2
			exit 1
			;;

	esac

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_remove_neovim_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_remove_neovim_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="remove neovim"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_systemd_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_systemd_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action=${1:-}

	case $action in
		-*) ;;

		loki)
			action="loki"
			shift
			gup_systemd_loki_parse_requirements "$@"
			shift $#
			;;

		promtail)
			action="promtail"
			shift
			gup_systemd_promtail_parse_requirements "$@"
			shift $#
			;;

		# :command.command_fallback
		"")
			gup_systemd_usage >&2
			exit 1
			;;

		*)
			printf "invalid command: %s\n" "$action" >&2
			exit 1
			;;

	esac

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_systemd_loki_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_systemd_loki_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="systemd loki"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.parse_requirements
gup_systemd_promtail_parse_requirements() {
	local key

	# :command.fixed_flags_filter
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in
			--help | -h)
				long_usage=yes
				gup_systemd_promtail_usage
				exit
				;;

			*)
				break
				;;

		esac
	done

	# :command.command_filter
	action="systemd promtail"

	# :command.parse_requirements_while
	while [[ $# -gt 0 ]]; do
		key="$1"
		case "$key" in

			-?*)
				printf "invalid option: %s\n" "$key" >&2
				exit 1
				;;

			*)
				# :command.parse_requirements_case
				# :command.parse_requirements_case_simple
				printf "invalid argument: %s\n" "$key" >&2
				exit 1

				;;

		esac
	done

}

# :command.initialize
initialize() {
	declare -g version="0.1.0"
	set -e

	# src/initialize.sh
	# default prefix (can be overridden)
	: "${GUP_PREFIX:=${HOME}/.local}"

	gup_ensure_deps() {
			local missing_deps=()
			for dep in git wget jq tar unzip; do
					if ! command -v "$dep" >/dev/null 2>&1; then
							echo "$dep"
							missing_deps+=("$dep")
					fi
			done

			if [ ${#missing_deps[@]} -ne 0 ]; then
					echo "Error: Missing dependencies: ${missing_deps[*]}" >&2
					exit 1
			fi
	}
	gup_ensure_deps
}

# :command.run
run() {
	# :command.globals
	declare -g long_usage=''
	declare -g -A args=()
	declare -g -A deps=()
	declare -g -a env_var_names=()
	declare -g -a input=()

	normalize_input "$@"
	parse_requirements "${input[@]}"

	case "$action" in
		"install") gup_install_command ;;
		"install gup") gup_install_gup_command ;;
		"install update") gup_install_update_command ;;
		"install go") gup_install_go_command ;;
		"install tinygo") gup_install_tinygo_command ;;
		"install nodejs") gup_install_nodejs_command ;;
		"install nvm") gup_install_nvm_command ;;
		"install deno") gup_install_deno_command ;;
		"install bun") gup_install_bun_command ;;
		"install uv") gup_install_uv_command ;;
		"install rust") gup_install_rust_command ;;
		"install zig") gup_install_zig_command ;;
		"install go-blueprint") gup_install_go_blueprint_command ;;
		"install gowebly") gup_install_gowebly_command ;;
		"install goplay") gup_install_goplay_command ;;
		"install gotests") gup_install_gotests_command ;;
		"install impl") gup_install_impl_command ;;
		"install gomodifytags") gup_install_gomodifytags_command ;;
		"install air") gup_install_air_command ;;
		"install gofumpt") gup_install_gofumpt_command ;;
		"install goimports") gup_install_goimports_command ;;
		"install golangci-lint") gup_install_golangci_lint_command ;;
		"install staticcheck") gup_install_staticcheck_command ;;
		"install nilaway") gup_install_nilaway_command ;;
		"install swag") gup_install_swag_command ;;
		"install templ") gup_install_templ_command ;;
		"install ruff") gup_install_ruff_command ;;
		"install scc") gup_install_scc_command ;;
		"install yamlfmt") gup_install_yamlfmt_command ;;
		"install gopls") gup_install_gopls_command ;;
		"install dlv") gup_install_dlv_command ;;
		"install zls") gup_install_zls_command ;;
		"install neovim") gup_install_neovim_command ;;
		"install helix") gup_install_helix_command ;;
		"install prometheus") gup_install_prometheus_command ;;
		"install grafana") gup_install_grafana_command ;;
		"install loki") gup_install_loki_command ;;
		"install node_exporter") gup_install_node_exporter_command ;;
		"install postgres_exporter") gup_install_postgres_exporter_command ;;
		"install blackbox_exporter") gup_install_blackbox_exporter_command ;;
		"install snmp_exporter") gup_install_snmp_exporter_command ;;
		"install btop") gup_install_btop_command ;;
		"install gotop") gup_install_gotop_command ;;
		"install lazydocker") gup_install_lazydocker_command ;;
		"install minikube") gup_install_minikube_command ;;
		"install kubectl") gup_install_kubectl_command ;;
		"install k9s") gup_install_k9s_command ;;
		"install etcd") gup_install_etcd_command ;;
		"install vagrant") gup_install_vagrant_command ;;
		"install doctl") gup_install_doctl_command ;;
		"install b2") gup_install_b2_command ;;
		"install githubcli") gup_install_githubcli_command ;;
		"install forgejocli") gup_install_forgejocli_command ;;
		"install caddy2") gup_install_caddy2_command ;;
		"install traefik") gup_install_traefik_command ;;
		"install hugo") gup_install_hugo_command ;;
		"install tailwindcss") gup_install_tailwindcss_command ;;
		"install marp") gup_install_marp_command ;;
		"install duf") gup_install_duf_command ;;
		"install gdu") gup_install_gdu_command ;;
		"install gum") gup_install_gum_command ;;
		"install lazygit") gup_install_lazygit_command ;;
		"install muffet") gup_install_muffet_command ;;
		"install s5cmd") gup_install_s5cmd_command ;;
		"install sshconfig") gup_install_sshconfig_command ;;
		"install task") gup_install_task_command ;;
		"install timer") gup_install_timer_command ;;
		"install ytdlp") gup_install_ytdlp_command ;;
		"install zfsnap") gup_install_zfsnap_command ;;
		"install terraform") gup_install_terraform_command ;;
		"install distrobuilder") gup_install_distrobuilder_command ;;
		"install upx") gup_install_upx_command ;;
		"install sqlc") gup_install_sqlc_command ;;
		"install mkcert") gup_install_mkcert_command ;;
		"install govulncheck") gup_install_govulncheck_command ;;
		"install nerdfonts") gup_install_nerdfonts_command ;;
		"remove") gup_remove_command ;;
		"remove neovim") gup_remove_neovim_command ;;
		"systemd") gup_systemd_command ;;
		"systemd loki") gup_systemd_loki_command ;;
		"systemd promtail") gup_systemd_promtail_command ;;
	esac
}

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
	# :command.start
	command_line_args=("$@")
	initialize
	run "${command_line_args[@]}"
fi
